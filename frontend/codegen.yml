overwrite: true
schema: "http://127.0.0.1:3000/graphql"
documents: "graphql/**/*.graphql"
generates:
  graphql/generated/:
    preset: "client"
    plugins: []
  ./graphql.schema.json:
    plugins:
      - "introspection"
